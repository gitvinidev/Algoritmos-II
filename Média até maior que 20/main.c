#include <stdio.h>

int main ()
{
	int F[50], som=0, i; 
	float med, c;
	 

    med=som/c;
    if (c=0) printf ("Divisao por zero");
    else if (!(c=0)) printf ("%.2f", med);   
     
           
           
	return 0;
}